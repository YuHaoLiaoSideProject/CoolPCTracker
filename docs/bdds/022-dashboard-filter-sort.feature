# language: zh-TW
功能: Dashboard 商品篩選與排序
  作為比價消費者
  我希望能篩選與排序 Dashboard 商品列表
  以便更快找到符合價格區間或品牌的商品

  背景:
    假設 商品列表已載入完成
    且 使用者停留在 Dashboard 頁面

  # ─── 排序 ────────────────────────────────────────────

  @happy-path @sort
  場景: 預設排序為價格由低到高
    假設 商品列表已載入
    且 無任何篩選條件
    那麼 商品應依目前價格由低到高排列
    且 第一張卡片為最便宜的商品

  @happy-path @sort
  場景: 切換排序為價格由高到低
    假設 商品列表已載入
    當 使用者在排序下拉選單選擇「價格高→低」
    那麼 商品應依目前價格由高到低排列
    且 第一張卡片為最貴的商品

  @happy-path @sort
  場景: 切換排序為最近更新
    假設 商品列表已載入
    當 使用者在排序下拉選單選擇「最近更新」
    那麼 商品應依 last_seen 日期由新到舊排列

  # ─── 價格區間篩選 ───────────────────────────────────

  @happy-path @filter-price
  場景: 輸入價格下限篩選
    假設 商品列表已載入
    當 使用者在價格下限輸入框輸入「5000」
    那麼 價格低於 5000 的商品應被排除
    且 價格 >= 5000 的商品應顯示

  @happy-path @filter-price
  場景: 輸入價格上限篩選
    假設 商品列表已載入
    當 使用者在價格上限輸入框輸入「15000」
    那麼 價格高於 15000 的商品應被排除
    且 價格 <= 15000 的商品應顯示

  @happy-path @filter-price
  場景: 同時輸入價格上下限
    假設 商品列表已載入
    當 使用者在價格下限輸入框輸入「5000」
    且 使用者在價格上限輸入框輸入「15000」
    那麼 價格在 5000 至 15000 之間的商品應顯示
    且 價格不在該區間的商品應被排除

  @edge-case @filter-price
  場景: 價格下限大於上限時自動交換
    假設 商品列表已載入
    當 使用者在價格下限輸入框輸入「15000」
    且 使用者在價格上限輸入框輸入「5000」
    那麼 系統應自動交換上下限
    且 價格在 5000 至 15000 之間的商品應顯示

  @edge-case @filter-price
  場景: 價格輸入非數字時忽略該值
    假設 商品列表已載入
    當 使用者在價格下限輸入框輸入「abc」
    那麼 該輸入應被忽略
    且 價格篩選不生效

  # ─── 品牌篩選 ────────────────────────────────────────

  @happy-path @filter-brand
  場景: 勾選單一品牌篩選
    假設 商品列表已載入
    且 該分類有以下品牌：「金士頓」「美光」「三星」
    當 使用者勾選品牌「金士頓」
    那麼 僅品牌為「金士頓」的商品應顯示
    且 其他品牌商品應被排除

  @happy-path @filter-brand
  場景: 勾選多個品牌篩選
    假設 商品列表已載入
    且 該分類有以下品牌：「金士頓」「美光」「三星」
    當 使用者勾選品牌「金士頓」
    且 使用者勾選品牌「美光」
    那麼 品牌為「金士頓」或「美光」的商品應顯示

  @happy-path @filter-brand
  場景: 取消勾選品牌
    假設 商品列表已載入
    且 使用者已勾選品牌「金士頓」
    當 使用者取消勾選品牌「金士頓」
    那麼 品牌篩選應不再生效
    且 所有品牌商品應顯示

  # ─── 多篩選交集 ──────────────────────────────────────

  @happy-path @filter-intersection
  場景: 排序 + 價格篩選交集
    假設 商品列表已載入
    當 使用者在排序下拉選單選擇「價格高→低」
    且 使用者在價格上限輸入框輸入「10000」
    那麼 價格 <= 10000 的商品應顯示
    且 商品應依價格由高到低排列

  @happy-path @filter-intersection
  場景: 價格篩選 + 品牌篩選交集
    假設 商品列表已載入
    當 使用者在價格下限輸入框輸入「3000」
    且 使用者勾選品牌「金士頓」
    那麼 價格 >= 3000 且品牌為「金士頓」的商品應顯示

  @happy-path @filter-intersection
  場景: 三重篩選交集
    假設 商品列表已載入
    當 使用者在排序下拉選單選擇「價格低→高」
    且 使用者在價格下限輸入框輸入「2000」
    且 使用者在價格上限輸入框輸入「20000」
    且 使用者勾選品牌「金士頓」
    那麼 價格在 2000 至 20000 且品牌為「金士頓」的商品應顯示
    且 商品應依價格由低到高排列

  # ─── 空狀態 ──────────────────────────────────────────

  @error-handling @empty-state
  場景: 篩選無結果時顯示空狀態
    假設 商品列表已載入
    當 使用者在價格上限輸入框輸入「100」
    那麼 應顯示空狀態訊息「無符合商品」
    且 應顯示「清除篩選」按鈕

  @error-handling @empty-state
  場景: 品牌篩選無結果時顯示空狀態
    假設 商品列表已載入
    且 該分類無品牌為「不存在品牌」的商品
    當 使用者勾選品牌「不存在品牌」
    那麼 應顯示空狀態訊息「無符合商品」
    且 應顯示「清除篩選」按鈕

  # ─── 清除篩選 ────────────────────────────────────────

  @happy-path @clear-filters
  場景: 點擊清除篩選重置所有條件
    假設 商品列表已載入
    且 使用者已設定價格篩選
    且 使用者已勾選品牌篩選
    當 使用者點擊「清除篩選」按鈕
    那麼 所有篩選條件應被清除
    且 商品列表應恢復為完整清單
    且 排序應恢復為預設（價格低→高）

  @happy-path @clear-filters
  場景: 篩選無結果時清除篩選恢復列表
    假設 商品列表已載入
    且 使用者已設定價格篩選導致無結果
    當 使用者點擊「清除篩選」按鈕
    那麼 所有篩選條件應被清除
    且 商品列表應恢復為完整清單

  # ─── 數量顯示 ────────────────────────────────────────

  @business-rules @count
  場景: 顯示符合篩選的商品數量
    假設 商品列表已載入
    當 使用者在價格下限輸入框輸入「5000」
    那麼 應顯示「顯示 N 件商品」（N 為符合條件的數量）

  @business-rules @count
  場景: 篩選前顯示總數量
    假設 商品列表已載入
    且 無任何篩選條件
    那麼 應顯示「顯示 N 件商品」（N 為該分類總商品數）

  # ─── 無商品時的分類 ──────────────────────────────────

  @edge-case @no-items
  場景: 該分類無商品時不顯示篩選控制項
    假設 該分類無任何商品
    那麼 排序下拉選單不應顯示
    且 價格篩選器不應顯示
    且 品牌篩選器不應顯示
